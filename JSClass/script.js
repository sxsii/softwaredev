let person = "Joshua"; //primitive value
let Person = "Joshua"; //object

let me = {
    name: "John Faith",
    age: 35,
    city: "Ogun"
};

//Java prefers camel casing. Nvm, only use the camel casing naming convention.
//be very descriptive with your naming, e'g, use myFirstJob instead of job one.

console.log('${me.name} is ${me.age} and lives in ${me.city}')